#!/usr/bin/env bash
# Buildpack « inline » de Weave — métadonnées de publication.
#
# Les types de processus sont déclarés dans le `Procfile`, qui fait autorité.
# Ce fichier ne sert qu'à satisfaire le contrat de l'API buildpack.

set -euo pipefail
cat <<YAML
---
config_vars: {}
YAML
