# Infrastructure locale : PostgreSQL et Redis, images officielles.
#
# Facultatif pour développer — l'API tourne aussi sur SQLite et un Redis local.
# Utile pour travailler dans les mêmes conditions qu'en production.

services:
  postgres:
    image: postgres:18-alpine
    container_name: weave-postgres
    environment:
      POSTGRES_USER: weave
      POSTGRES_PASSWORD: weave
      POSTGRES_DB: weave
      # Sans cela, l'initialisation utilise la locale du conteneur.
      LANG: fr_FR.utf8
    ports:
      - "5432:5432"
    volumes:
      # PostgreSQL 18+ range ses données dans un sous-dossier propre à la
      # version majeure : le point de montage est le dossier parent, pas
      # `/data`. Monter `/data` fait refuser le démarrage à l'image officielle.
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U weave -d weave"]
      interval: 5s
      timeout: 3s
      retries: 10

  redis:
    image: redis:8-alpine
    container_name: weave-redis
    command:
      - redis-server
      - --save
      - "300"
      - "10"
      - --appendonly
      - "no"
      # Toute clé porte un TTL : en cas de pression mémoire, on évince
      # d'abord ceux qui expirent le plus tôt, jamais une clé sans expiration.
      - --maxmemory-policy
      - volatile-ttl
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  postgres-data:
  redis-data:
